## Guide for running from parent directory with config.yaml setup, use --no-venv if needed:
```bash
nox -s run 
```
## For linting and formatting and run use:
```bash
nox
```
## Directyly can run
```bash
python -m main.main
```